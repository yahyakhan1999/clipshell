# ClipShell

A Go-based developer tool that executes shell commands, captures stdout/stderr, and automatically copies formatted command output to the clipboard.

## Example

```bash
clipshell "git status"
